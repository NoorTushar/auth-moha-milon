import { useContext } from "react";
import { AuthContext } from "../providers/AuthProvider";

const Home = () => {
   return (
      <div>
         <h3>Home</h3>
      </div>
   );
};

export default Home;
